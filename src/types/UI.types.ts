export type StatCardProps = {
  label: string;
  value: string;
  className?: string;
};

export type ClickPopup = {
  id: string;
  value: number;
  x: number;
  y: number;
};

export type ResourceSpendFeedback = {
  id: number;
  amount: number;
};
